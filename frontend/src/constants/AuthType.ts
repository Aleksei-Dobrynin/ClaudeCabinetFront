import { Dayjs } from "dayjs";

export type AuthType = {
  
  id: number;
  description: string;
  name: string;
  code: string;
};


export type AuthTypeCreateModel = {
  
  id: number;
  description: string;
  name: string;
  code: string;
};

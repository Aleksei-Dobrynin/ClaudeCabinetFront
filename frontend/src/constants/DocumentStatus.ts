import { Dayjs } from "dayjs";

export type DocumentStatus = {
  
  id: number;
  descriptionKg: string;
  textColor: string;
  backgroundColor: string;
  name: string;
  description: string;
  code: string;
  nameKg: string;
};


export type DocumentStatusCreateModel = {
  
  id: number;
  descriptionKg: string;
  textColor: string;
  backgroundColor: string;
  name: string;
  description: string;
  code: string;
  nameKg: string;
};
